# Dependências de RUNTIME (o que a função da Vercel realmente precisa).
# Mantido enxuto de propósito: a Vercel embute tudo que está aqui no bundle
# da função (limite de 500MB, sem tree-shaking automático para Python).
fastapi==0.117.1
pydantic==2.9.2
langgraph==0.2.60
scikit-learn==1.6.1
numpy==2.1.3
scipy==1.14.1
requests==2.32.3
python-dotenv==1.0.1
